; inherits: hcl
; Lexical name bindings for Terraform (lexical-name-resolution ADR).
;
; Terraform is HCL; the inherited rules already speak its address
; vocabulary (var/local/module/data/resource). Nothing to add.
